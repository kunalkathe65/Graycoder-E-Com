import React from 'react';

import Base from '../core/Base';

const AddCategory = () => {
  return (
    <Base title='Add New Category' description='Category for everyone'></Base>
  );
};

export default AddCategory;
